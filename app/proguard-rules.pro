# MotionFlow R8 configuration.
#
# The application currently has no reflection-based code, so no keep rules are
# required. Rules for media, JNI and inference components are added in the
# phases that introduce them.
